name: Deploy Backend to Azure

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: windows-latest 

    steps:
    - name: Kodu İndir (Checkout)
      uses: actions/checkout@v4

    - name: .NET Kur
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.x' # Projen .NET 7 ise burayı 7.x yapmayı unutma

    # 🔥 DÜZELTİLEN KISIM BURASI: Robota tam olarak hangi dosyayı derleyeceğini söyledik
    - name: Projeyi Derle (Build)
      run: dotnet build SmartAttendance.WebApi/SmartAttendance.WebApi.csproj --configuration Release

    - name: Projeyi Paketle (Publish)
      run: dotnet publish SmartAttendance.WebApi/SmartAttendance.WebApi.csproj -c Release -o ./publish_output

    - name: Azure'a Yükle (Deploy)
      uses: azure/webapps-deploy@v3
      with:
        app-name: 'smartattendance-ffhxgvbsd6h7ancr' 
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ./publish_output